# Data 4 Help

## Building

```sh
    mvn clean install
```

## Test Deploy
```sh
    docker-compose up --build
```

## Delete composition
```sh
    docker-compose rm 
```

## Delete volumes
```sh
    docker volume ls
    docker volume rm name volume
```